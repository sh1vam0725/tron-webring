interface WebringNavProps {
  onRandom: () => void;
}

const WebringNav = ({ onRandom }: WebringNavProps) => {
  return (
    <div className="flex items-center justify-center font-mono text-sm">
    </div>
  );
};

export default WebringNav;
